## Валентинка-сайт 💌

Маленький сайт-приглашение на свидание с красивым оформлением (анимированные сердечки, стеклянная карточка и т.д.).

### Как запустить

1. Установите зависимости (в корне проекта):

```bash
npm install
```

2. Запустите сервер:

```bash
npm start
```

3. Откройте в браузере:

```text
http://localhost:3000
```

### Запуск в Docker (Node + Postgres)

1. Установите Docker и Docker Compose.
2. В корне проекта выполните:

```bash
docker-compose up --build
```

3. Откройте в браузере:

```text
http://localhost:3000
```

В этом режиме:

- контейнер `db` поднимает Postgres с БД `valentine`;
- контейнер `app` запускает Node-приложение и подключается к БД через переменные окружения из `docker-compose.yml`;
- таблица `responses` создаётся автоматически при старте сервера.

### Проверка состояния backend/DB

После запуска можно проверить состояние API и БД:

```text
http://localhost:3000/api/health
```

Если всё ок, вернётся JSON со статусом `ok`.

### Deploy на Render (Web Service + Postgres)

Для Web Service в Render задайте переменную окружения с URL базы:

- `DATABASE_URL` (рекомендуется), либо
- `EXTERNAL_DATABASE_URL`, `EXT_DATABASE_URL`, `RENDER_EXTERNAL_DATABASE_URL`, `DB_URL`, `POSTGRES_URL`.

Значение — ваш External Database URL вида:

```text
postgresql://user:password@host:5432/database
```

После деплоя проверьте:

```text
https://<your-render-app>.onrender.com/api/health
```

### Как подставить имя конкретного человека

В файле `public/script.js` в самом верху есть строка:

```js
const PERSON_NAME = 'Твоё имя здесь';
```

Поменяйте `'Твоё имя здесь'` на нужное имя – оно появится в заголовке валентинки.

### Как это работает

- **Backend**: `server.js` на Express
  - Отдаёт статические файлы из папки `public`
  - Принимает ответы по `POST /api/response` (поле `answer` = `"yes"` или `"maybe"`, плюс необязательное `message`)
  - Логирует ответы в консоль и хранит последний ответ в памяти

- **Frontend**: файлы в `public/`
  - `index.html` – разметка и тексты
  - `styles.css` – оформление, фон, анимация сердечек, карточка
  - `script.js` – логика кнопок, отправка ответа на backend, тосты и модальное окно
